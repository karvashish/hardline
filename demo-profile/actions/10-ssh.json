{
  "steps": [
    {
      "id": "sshd-config-perms",
      "plugin": "file_meta",
      "config": {
        "path": "/etc/ssh/sshd_config",
        "mode": "0600",
        "owner": "root",
        "group": "root"
      }
    },
    {
      "id": "ssh-hardening-drop-in",
      "plugin": "template",
      "config": {
        "src": "templates/10-ssh-hardening.tmpl",
        "dest": "/etc/ssh/sshd_config.d/99-hardline-demo.conf",
        "mode": "0600"
      }
    },
    {
      "id": "ssh-service-reload",
      "plugin": "service",
      "config": {
        "name": "ssh",
        "enabled": true,
        "state": "reloaded",
        "restart_policy": {
          "type": "on_change",
          "steps": [
            "ssh-hardening-drop-in"
          ]
        }
      }
    }
  ]
}
